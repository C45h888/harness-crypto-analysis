aiohttp==3.12.15
asyncpg==0.30.0
redis[hiredis]==6.4.0
alembic==1.13.3
psycopg2-binary==2.9.12
# NOOA base runtime and CLI. The analyst suite and `market` harness group are
# mounted through harness.py and the repo-root `nooa` CLI; canonical
# collection, calculation, persistence, and execution stay outside it.
# 0.0.8 is the same release the dev venv resolves from PyPI (wheels means no
# git/build toolchain is required in the Docker image).
nooa==0.0.8
nooa-cli==0.0.8